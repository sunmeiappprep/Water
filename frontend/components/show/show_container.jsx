import { connect } from "react-redux";
import Show from "./show";

const mSTP = (state) => {
    return {
        
    };
};

const mDTP = dispatch => ({
    // 
});

export default connect(mSTP, mDTP)(Show);